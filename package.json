{
  "name": "info-filter-jev",
  "version": "0.4.8",
  "private": true,
  "type": "module",
  "description": "信息过滤器 · Jev —— Chrome MV3 扩展：用 TypeSafe Jev / System One 的类型化决策过滤 x.com 时间线上的色情引流、诈骗、广告导流、标题党、低质 AI 与刷屏农场",
  "scripts": {
    "test": "node --test tests/",
    "check": "node tools/check-sources.js",
    "icons": "node tools/make-icons.js",
    "verify": "node tools/verify-in-chrome.js",
    "all": "npm run check && npm test && npm run verify",
    "package": "node tools/package.js",
    "release": "npm run all && npm run package"
  },
  "engines": {
    "node": ">=20"
  }
}
